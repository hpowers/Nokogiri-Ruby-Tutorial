<!DOCTYPE HTML>
<html lang="en-US">
<head>
	<meta charset="UTF-8">
	<title>9:30 Show</title>
	<link rel="stylesheet" href="http://current.bootstrapcdn.com/bootstrap-v204/css/bootstrap-combined.min.css">
</head>
<body>
	<div class="span8">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Date</th>
            <th>Time</th>
            <th>Event</th>
            <th>Price</th>
          </tr>
        </thead>
        <tbody>
        <% @concerts.each do | concert | %>
          <tr>
            <td><%= concert[:date]  %></td>
            <td><%= concert[:time]  %></td>
          <% unless concert[:soldout] %>
            <td>
              <a href="<%= concert[:buy] %>">
                <%= concert[:title] %>
              </a>
            </td>
            <td><%= concert[:price] %></td>
          <% else %>
            <td><del><%= concert[:title] %></del></td>
            <td>SOLD OUT</td>
          <% end %>
        </tr>
      <% end %>
        </tbody>
      </table>
    </div>
</body>
</html>
